# Copy to .env and fill in your values.
# MongoDB Atlas: Cluster0 → Connect → Drivers → copy the connection string.
# Replace <password> with your DB user password. The host must be your real cluster
# host (e.g. cluster0.abc12.mongodb.net), NOT cluster0.xxxxx.mongodb.net.

PORT=4000
MONGODB_URI=mongodb+srv://appdev_db_user:IdrIxpyeGuHhzhn0@cluster0.ed9fjps.mongodb.net/?appName=Cluster0
JWT_SECRET=your-super-secret-jwt-key-change-in-production
NODE_ENV=development

# Twilio Configuration (for OTP verification)
TWILIO_ACCOUNT_SID=your_twilio_account_sid
TWILIO_AUTH_TOKEN=your_twilio_auth_token
TWILIO_PHONE_NUMBER=+1234567890
